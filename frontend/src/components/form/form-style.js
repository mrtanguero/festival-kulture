import { makeStyles } from '@material-ui/core/styles'


const colors = {
    formBtn: ''
}

export const useStyles = makeStyles({
    formContainer:{
        display: 'flex',
        flexDirection: 'column',
        width: '300px',
        margin: 'auto',

    },

    formRoot: {
        display: 'flex',
        flexDirection: 'column'

    }
})